PyQt6>=6.7.0
sounddevice>=0.4.6
numpy>=1.26.0
pynput>=1.7.6
groq>=0.4.0
requests>=2.31.0
python-dotenv>=1.0.0
keyring>=24.0.0
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-Quartz>=10.0
# Accessibility probe (AXIsProcessTrusted). Pulled in transitively by Quartz
# today, but core/permissions.py imports it directly — pin it so a future pyobjc
# reshuffle can't drop the edge and silently break the permission check.
pyobjc-framework-ApplicationServices>=10.0

# --- Local offline transcription (Apple Silicon / Apple MLX) ---
# Se BUNDLEAN en el .app (ver sflow.spec) para que los modelos locales funcionen
# sin instalacion manual. En Intel Mac fallan al importar y el router cae a Groq.
# Benchmark M4 (voz real es, 12-jul-2026): whisper-large-v3-turbo ~950ms/WER 2.9%
# (mejor precision) · parakeet v3 ~280ms (mas rapido). Ambos seleccionables en Ajustes.
mlx-whisper>=0.4.0
parakeet-mlx>=0.3.0
# Local LLM cleanup (offline provider). Optional at runtime — the Qwen 4-bit
# weights are an on-demand download, and cleanup is fail-open, so a missing model
# just means the raw transcript is pasted.
mlx-lm>=0.19.0
# Model weight management (ModelManager: availability check + on-demand download
# with progress). Already a transitive dep of mlx-whisper; pinned here because
# core/models.py imports it directly (snapshot_download).
huggingface_hub>=0.24.0
